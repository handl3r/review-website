# Requirements:
* docker, docker-compose
# Instructions:
* /# docker-compose up
* /# docker-compose exec web bash
    * rails db:create
    * rails db:migrate
* /# rails console for interact wic th model, database